"""African elections frame analysis — pipeline + eval + analysis modules.

Four-stage pipeline:
    ingest (data.py) -> clean (cleaning.py) -> classify (classify.py) -> eval

Analysis and figures: viz.py. Diagnostic helpers for notebook decision blocks:
diagnostics.py.

See ``README.md`` at the project root for an overview.
"""

__version__ = "0.1.0"
